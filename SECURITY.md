# Security Policy

## Supported Versions

安全修复优先应用于默认分支的最新版本。尚未发布的旧提交和第三方 Fork 不保证获得安全更新。

| Version | Supported |
| --- | --- |
| Default branch | Yes |
| Older snapshots | No |

## Reporting a Vulnerability

请不要通过公开 Issue、Discussion、Pull Request 或社交媒体披露未修复的漏洞。

优先使用仓库的 GitHub Private Vulnerability Reporting。若该功能尚未启用，请通过仓库所有者公开资料中列出的私密安全联系渠道报告。报告中请提供：

- 受影响的版本或提交；
- 可复现的最小步骤；
- 预期行为、实际行为和潜在影响；
- 不包含真实候选人数据、令牌或其他个人信息的概念验证；
- 可行的缓解建议（如有）。

维护者会尽量在 5 个工作日内确认收到报告，并在验证后沟通修复和披露计划。请在双方约定公开时间前保密。

如果报告中意外包含了凭证或个人信息，请立即停止继续传播，并在安全渠道中说明需要撤销、轮换或删除的材料。

## Security Model

AI Talent Radar 是单用户、本地优先的应用：

- 原生启动默认仅监听 `127.0.0.1`；容器镜像在容器内部监听 `0.0.0.0`，而随附 Compose 配置只把端口发布到宿主机的 `127.0.0.1`；
- Web 界面没有账号体系或远程访问鉴权；
- SQLite 数据库、备份和导出文件保存在运行设备上；
- 采集任务会向已实现来源的公开接口或公开页面发送请求；
- 应用不需要浏览器 Cookie、密码或私人登录会话。

因此，不应直接把服务暴露到公网、局域网共享主机或不可信反向代理。需要多人使用时，应先增加身份认证、访问控制、TLS、审计日志和数据隔离。

## Operator Responsibilities

- 使用受支持的 Python、Node.js 和容器基础镜像，并及时更新安全补丁。
- 仅通过操作系统凭证管理工具或部署平台 Secrets 提供令牌。
- GitHub Token 采用最小权限；无需写权限时不得授予写权限。
- 限制数据库、备份、报告和导出文件的文件系统权限。
- 对含个人信息的备份启用磁盘加密，并制定保留与销毁策略。
- 在提交、发布或构建镜像前运行仓库 CI 的隐私与敏感文件检查。
- 发现凭证进入提交历史后立即撤销或轮换；仅删除当前文件并不足以消除泄露。

## In-Scope Examples

- 未授权读取、修改或删除本地候选人数据；
- 路径遍历、脚本注入、公式注入或不安全外部链接；
- API 边界校验缺失导致的数据破坏；
- 凭证或个人信息被写入日志、报告、导出、镜像或版本库；
- 默认配置意外监听非回环网络接口。

平台封禁、验证码绕过、速率限制规避和针对第三方来源的攻击不属于本项目支持范围，也不会接受相关实现。

## Safe Testing

仅使用合成数据、临时数据库和 `.example` / `.test` 保留域名测试。不得对真实候选人、第三方账号或生产系统进行未经授权的安全测试。测试应避免产生大量外部请求，不得尝试绕过来源平台的访问控制。
